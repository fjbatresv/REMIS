{%extends '::base.html.twig'%}
{% block title %}{%if pk%}Editar{%else%}Nueva{%endif%} sede{% endblock %}
{%block body%}
    <!-- left column -->
    <div class="col-md-12">
        <!-- general form elements -->
        <div class="box box-primary">
            <div class="box-header">
                <h3 class="box-title">{%if pk%}Editar{%else%}Nueva{%endif%} sede</h3>
            </div><!-- /.box-header -->
            <!-- form start -->
            <form method="POST" action="{{path(ruta, {'pk' : pk})}}" novalidate="">
                <div class="box-body">
                    <div class="row form-group">
                        <div class="col-md-2">
                            {{form_label(form.nombre)}}
                        </div>
                        <div class="col-md-4">
                            {{form_widget(form.nombre)}}
                            <span class="has-error">
                                {{form_errors(form.nombre)}}
                            </span>
                        </div>
                    </div>
                    <div class="row form-group">
                        <div class="col-md-2">
                            {{form_label(form.direccion)}}
                        </div>
                        <div class="col-md-4">
                            {{form_widget(form.direccion)}}
                            <span class="has-error">
                                {{form_errors(form.direccion)}}
                            </span>
                        </div>
                    </div>
                    <div class="row form-group">
                        <div class="col-md-2">
                            {{form_label(form.telefono)}}
                        </div>
                        <div class="col-md-4">
                            {{form_widget(form.telefono)}}
                            <span class="has-error">
                                {{form_errors(form.telefono)}}
                            </span>
                        </div>
                    </div>
                    <div class="row form-group">
                        <div class="col-md-2">
                            {{form_label(form.tipo)}}
                        </div>
                        <div class="col-md-4">
                            {{form_widget(form.tipo)}}
                            <span class="has-error">
                                {{form_errors(form.tipo)}}
                            </span>
                        </div>
                    </div>
                    {{form_rest(form)}}
                </div>

                <div class="box-footer">
                    <button type="submit" class="btn btn-primary"><i class="fa fa-save"></i> Guardar</button>
                    <a  href="{{path('Invision_SoporteBundle_Sede_list')}}" class="btn btn-danger"><i class="fa fa-undo"></i> Regresar</a>
                </div>
            </form>
        </div>
    </div>

{%endblock%}
